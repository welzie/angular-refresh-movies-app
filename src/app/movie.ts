export class Movie {
  title: string;
  year: number;
  category: string;

  constructor(title: string, year: number, category: string) {
    this.title = title;
    this.year = year;
    this.category = category; 
  }
  
}
